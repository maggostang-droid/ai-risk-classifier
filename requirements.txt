# Für Streamlit Community Cloud: installiert Runtime-Dependencies aus
# pyproject.toml plus das eigene Package (-e .). Lokal reicht
# `pip install -e ".[dev]"` (siehe CLAUDE.md).
langchain>=0.3
langchain-anthropic>=0.3
langchain-openai>=0.2
python-dotenv>=1.0
streamlit>=1.38
-e .
